Imports System.Configuration
Imports System.Data.SqlClient
Imports DataAccessHandler
Public Class frmRegister
    Public strCaller As String
    Dim strKey As String

    Private Sub frmRegister_Load(sender As Object, e As EventArgs) Handles Me.Load
        'txtMessage = "This is the Evaluation Version of " &
        '       "SalaryPro .  If you want to upgrade " &
        '       "to the Licenced Version, please contact " &
        '       "us for the Installation Key.  Please " &
        '       "include the Demo Key (" & strGKey &
        '       ") with the correspondence." & vbCrLf & vbCrLf &
        '       "If you want to continue evaluating the " &
        '       "product, please press the [Register Later] " &
        '       "button."
    End Sub

    Private Sub btnUpdate_Click(sender As Object, e As EventArgs) Handles btnUpdate.Click
        'Dim strValue As String
        'Dim strKey As String
        'Dim strInstKey As String
        'Dim strEntKey As String
        'Dim bytType(4) As Byte
        'Dim i As Integer
        'Dim sFile As String

        ''For i = 0 To 3
        ''    strKey = strKey + Mid(strGKey, i * 5 + 1, 4)
        ''Next

        ''strValue = Mid(strKey, 9, 1)
        ''strKey = TransformKey(strKey, strValue, 0)
        ''For i = 0 To 3
        ''    strInstKey = strInstKey + Mid(strKey, i * 4 + 1, 4) + IIf(i < 3, "-", "")
        ''Next

        ''strEntKey = txtKey(0) + "-" + txtKey(1) + "-" + txtKey(2) + "-" + txtKey(3)
        ''If strEntKey = strInstKey Then
        ''    sFile = "C:\Program Files\Seagate Software\Shared\msnixbup.dll"
        ''    Open sFile For Binary As #1
        ''For i = 0 To 3
        ''        bytType(i) = Asc(Mid("CD53", i + 1, 1))
        ''    Next
        ''    Put #1, 10801, bytType
        ''Close #1
        ''MsgBox "You have successfully registered SalaryPro", vbInformation, strProject
        ''Unload Me
        ''strInstType = "CD53"
        ''    If strCaller = "" Then
        ''        bButtonClicked = True
        ''        frmCompany.Show
        ''    End If
        ''Else
        ''    MsgBox "Installation key is incorrect." & vbCrLf &
        ''        "Please try again.", vbCritical, strProject
        ''txtKey(0).SetFocus
        ''End If
    End Sub

    Private Sub cmdLater_Click(sender As Object, e As EventArgs) Handles cmdLater.Click
        'Unload Me
        'If strCaller = "" Then
        '        CheckCopy
        '        frmCompany.Show
        '    End If
    End Sub

    Private Sub btnExit_Click(sender As Object, e As EventArgs) Handles btnExit.Click
        End
    End Sub
End Class